import React from 'react';
import {
  View,
  Text,
  StyleSheet,
  Image,
  TouchableOpacity,
  SafeAreaView,
} from 'react-native';
import {COLORS, images, SIZES, FONTS, icons} from '../constants';
import LinearGradient from 'react-native-linear-gradient';

const Flight = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Hello Flight!</Text>
      </View>
    </SafeAreaView>
  );
};

export default Flight;
